export type CompanyInfo = {
  name: string;
  ceo: string;
  description: string;
  address: string;
  image: string;
};

export type TodosList = {
  id: string;
  title: string;
  content: string;
  isDone: boolean;
};
